const SimpleStorage = artifacts.require("SimpleStorage");

contract("Contract Tests", (accounts) => {
  beforeEach(async () => {
    console.log("beforeEach");
  });

  it("Let users stake", async () => {
    console.log("Let users stake");
  });
});
